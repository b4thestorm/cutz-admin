'use client'
import { Container, Typography} from "@mui/material";


export default function Home() {
  return (
    <Container>
      <Typography variant={"h1"}>Cutz</Typography>
      <Typography variant={"h3"}>Simple utility to make getting haircuts too easy</Typography>
    </Container>
  );
}
